server:
    port: 8081

logging:
    level:
        org.springframework.security: DEBUG
spring:
  application:
    name: springboot-tipjs
  datasource:
    url: jdbc:mysql://localhost:3306/estateadvance
    username: root
    password: 123123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true